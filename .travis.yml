language: python
python:
  - "3.5"
install:
  - pip install --upgrade pip
  - pip install pipenv
  - pipenv install --dev
script:
  - pipenv run flake8 .
  - pipenv run pytest
notifications:
  email:
    - felipe.gomes.ferreira@gmail.com